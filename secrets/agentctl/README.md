Repo-local credential staging for `scripts/agentctl.py`.

Expected files:

- `codex/auth.json`
- `codex/config.toml`
- `claude/settings.json`

These files are intentionally ignored by git.
Create them locally and fill them with your own credentials before deploying.

Starter files are created empty on purpose.
